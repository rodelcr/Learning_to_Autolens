# Handover — 2026-04-27

State snapshot. Half-day cycle: P3 mge re-fit (v2), P9 subhalo Cannon
results landed, two new canonical curriculum modules shipped (12 + 13
were already in; 13 + 14 written today, anchored on new Mathematica
symbolic derivations and rigorously-verified citations). The user
emphasised "be very wary of any extrapolations from Claude" mid-day —
the response was a verification-agent audit of every claim in 13 + 14
before writing anything. Several would-be misstatements caught.

---

## 1. Examples/ collection — current status (11 entries, no ◯ stubs)

| # | Example | Status | Notes |
|---|---|---|---|
| 1 | `compound_lens` | ✓ shipped + 03 advanced | EPL canonical: chi²/N=0.661, max\|res\|=3.82σ. |
| 2 | `disky_spiral_lens` | ✓ shipped | Bayes factor e^{61,969}. |
| 3 | `double_source_plane` | ✓ shipped | log_Z=+29,014. |
| 4 | `group_scale` | ◐ in-progress | 3 Cannon attempts stalled. (Carryover.) |
| 5 | `quad_time_delay` | ◐ in-progress | P2 H0 max-lik=74.6 vs truth 70 (MSD demo). |
| 6 | `mge_to_physical` | ◐ in-progress | **P3 v2 results landed today** — see §2. |
| 7 | `compound_lens_zoo` | ◐ in-progress | mock_5 redo committed clean. |
| 8 | `bayesian_model_comparison` | ◐ in-progress | **+ v2 + subhalo rows added today**. |
| 9 | `interferometer_basic` | ◐ in-progress | SMA visibility-plane fit shipped. |
| 10 | `subhalo_sensitivity` | ◐ in-progress (scaffold) | **P9 results landed today** — see §2. |
| 11 | `agel_real_target` | ◐ in-progress (capstone, scaffold) | P10 first pass landed yesterday. |

## 1b. Modules/ canonical curriculum (14 entries now)

| # | Module | Status |
|---|---|---|
| 01-11 | (existing) | shipped + Mathematica derivations 01-09 |
| 12 | Time-Delay Cosmography & MSD | shipped (yesterday) |
| 13 | **TDCOSMO with Kinematics** | **NEW today** + `Mathematica/13_jeans_and_kinematics.wl` |
| 14 | **Compound (Multi-Plane) Lensing** | **NEW today** + `Mathematica/14_multi_plane.wl` |

---

## 2. Headline results — P3 v2 + P9

### P3 mge_to_physical v2 — model misspecification fixed

The v1 canonical fit had a +288 Bayes factor (Search 3 stars+DM vs Search 2 stars-only) but ~10σ ring residuals. Caveat added in yesterday's work attributed this to model misspecification (omitted secondary deflector + omitted second source). v2 driver added both back — Cannon job 8746085 ran the chain.

| | log_Z | χ²/N | Verdict |
|---|---|---|---|
| Search 2 v1 (misspecified) | +22,653.7 | 3.13 | FAIL |
| Search 2 v2 (2-src + secondary) | **+28,415.4** | **1.87** | borderline SUSPECT (down from FAIL) |
| Search 3 v1 (misspecified) | +22,941.5 | 3.06 | FAIL |
| Search 3 v2 (+NFW dark) | **+28,455.7** | **1.85** | SUSPECT (chi²/N still > 1.3) |

**Key Bayes factor pivot:** Search 3 v2 vs Search 2 v2 = **+40** log units. *NOT +288.*

The +288 was almost entirely absorption of unmodelled flux. The genuine physical content of the DM constraint on this mock is +40 — *strong* on Trotta's scale but *not decisive*. P5 `bayesian_model_comparison/` table now records both numbers side-by-side as a textbook §5.4 model-misspecification demonstration.

Note: slurm reported the v2 job as FAILED (the same `xp=` autolens internal-API bug crashed Search 3 v2's post-fit visualize step — same as v1). Nautilus completed and `samples_summary.json` was written before the crash, so the fit is valid; only the visualize step bombed.

### P9 subhalo_sensitivity — sensitivity-threshold signature

Cannon job 8746086 ran the two-fit Bayes-factor demo on the M_200=1e9 perturber mock (~at the Vegetti+ 2014 detection threshold for HST-class data).

| | log_Z | χ²/N | max\|res\| |
|---|---|---|---|
| smooth | +22,587.4 | 1.11 | 6.32σ |
| with_subhalo | +22,585.8 | 1.11 | 6.32σ |

**Δlog_Z = −1.6 favouring SMOOTH** — i.e. the perturber is NOT detected. Posterior on `mass_at_200` collapsed to median ~5.6×10⁷ Msun (noise-floor scale, far below truth 10⁹). This is exactly the *non-detection* signature — sensitivity-threshold demonstration consistent with Vegetti+ 2014. P5 records it as a row.

### Modules 13 + 14 with extra rigor

User said "be very wary of any extrapolations". Verification protocol applied:

1. **Spawned audit agent** to verify every claim against arXiv / NASA ADS / journal sites BEFORE writing.
2. **Caught misstatements:** anisotropic vs isotropic Jeans wording, my faulty MSD-σ_v reasoning ("uniform sheet contributes to enclosed mass" — wrong by Birkhoff), Keeton 2001 arXiv ID (0102341 not 0102340), autolens code path (`tracer_util.py` not `tracer.py`), multi-plane Fermat cross-term structure.
3. **Wrote new Mathematica** `13_jeans_and_kinematics.wl` and `14_multi_plane.wl` symbolically deriving each formula. Both run cleanly via `wolframscript`.
4. **Module 14 directly cites + prints** the exact PyAutoLens code lines (`tracer_util.py:101-180`) — no claims about implementation that aren't directly readable.
5. Both notebooks execute cleanly via nbconvert.

---

## 3. Outstanding TODO (priority-ordered for next session)

### Priority 1 — Solutions for Modules 12 / 13 / 14

User explicitly added this post-handoff. Mirror existing `Solutions/` patterns. Each Solutions notebook should:
- Mirror the parent module's structure
- Provide worked answers to every exercise
- Cross-reference the relevant .wl file for any math
- Cite only the *verified* BibTeX entries from the parent module's reference list — same "be wary of extrapolations" standard

### Priority 2 — P3 mge re-fit *Search 3 v2* without the autolens xp= bug

The v2 numbers we have are valid (Nautilus completed before the visualize crash) but it would be cleaner to either:
- patch the autolens `MassProfile.potential_2d_from()` signature to accept an `xp=` kwarg, OR
- avoid the affected code path (e.g. set `force_visualize_overwrite=False` or skip the `image/` outputs)

This would let Search 3 v2 produce a clean `fit_subplot.png` (currently absent).

### Priority 3 — Real DM detection at higher mass

P9 subhalo at M_200=1e9 was below the detection threshold. Re-generate the mock at 1e10 or 1e11 Msun and re-submit. Should give Δlog_Z > +10 (decisive), validating the methodology.

### Priority 4 — `agel_real_target` PSF fix

The placeholder-PSF failure (max|res|=32σ at lens centre) is the obvious next fix. README §Caveat 1 + Exercise 2 spell out the recipe: empirical PSF stacking from 5 stars in the segment catalog.

### Priority 5 — Resume `group_scale` (long carryover)
### Priority 6 — Module 15 candidate

Possibilities: cluster-scale lensing (gNFW + LOS halos), interferometer multi-band, MGE source science. None pressing.

---

## 4. Today's commits (after the 2026-04-26 handoff)

```
43812de mge_to_physical driver: add Search 2/3 v2 with 2-source + secondary deflector
6f5610d (yesterday's modules — already covered in HANDOFF 04-26)
03e6c5a (handoff 04-26)
[today] Module 12 work was already done; 13 + 14 added today + .wl scripts
[today] P3 v2 + P9 results commit
[today] P5 worked-examples table updated with v2 + subhalo rows
```

Use `git log --oneline 03e6c5a..HEAD` to see the precise list at handoff time.

---

## 5. Cannon environment + workflow

Same as always. Re-auth needed every ~3h (Duo). When you re-open a session:

```bash
ssh cannon "echo ok"   # triggers Duo, establishes multiplex
ssh cannon "squeue -u $USER"
bash Modules/10_Cluster_Computing/scripts/pull_from_cannon.sh --go
```

---

## 6. First actions after the next /compact

1. `git status` — clean.
2. Read `LEARNING_LOG.md` — the 2026-04-26 entry on Bayes-factor-without-clean-residual is the most actionable.
3. Pick from §3 priority list.
4. **P1 (Solutions for Modules 12-14)** is the user's explicit ask. Likely the natural next move.

---

## 7. File index

| What | Where |
|---|---|
| Today's handover | `Modules/10_Cluster_Computing/HANDOFF_2026_04_27.md` (this) |
| Yesterday's | `Modules/10_Cluster_Computing/HANDOFF_2026_04_26.md` |
| Earlier handovers | `HANDOFF_2026_04_25.md`, `HANDOFF_2026_04_24.md`, `HANDOFF_2026_04_22.md` |
| Engineering log | `LEARNING_LOG.md` |
| Examples roadmap | `Examples/README.md` |
| Mathematica derivations | `Mathematica/01_*.wl` … `Mathematica/14_*.wl` |
| Lenstronomy → autolens conventions | `Examples/mge_to_physical/mocks/PROVENANCE.md` |
| Bayes-factor references | `Examples/bayesian_model_comparison/README.md` |
| Real-data caveats template | `Examples/agel_real_target/README.md` §Caveats |
