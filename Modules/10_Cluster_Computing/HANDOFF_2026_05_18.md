# Handover — 2026-05-18

State snapshot for compact + next-session restart. Continues from `HANDOFF_2026_05_15.md` (3 days prior).

---

## 1. Headline since 2026-05-15

### v0.97 substantive work shipped on `main`

- **`radial_arc_smbh --part=with_kinematics`** driver wired against Phase 3 `_jeans_sigma_v.py` + `AnalysisKinematics`. After fixing an autofit `AnalysisFactor.save_attributes` API gap (commit `36b68e4`), Cannon job **13649652 RUNNING** for the γ′–M_BH kinematic-break demo.
- **`qtd --part=joint_fit_h0_kin`** driver written + smoke-tested locally (γ′ sensitivity confirmed: Δlog_L=+1.96 at γ=2.15 vs truth). Three-factor `FactorGraphModel(point, imaging, kinematic)`. Cannon job **13649870 RUNNING** for the TDCOSMO MSD-break demo.
- **`Examples/quad_time_delay/mocks_with_host/sigma_v_dataset.json`** generated via Phase 3 Jeans solver — σ_v_truth = 283.86 km/s; σ_v_obs = 282.44 ± 10 km/s (seed 99).
- **`AnalysisKinematics` API stubs** (commit `36b68e4`) — added `save_attributes`, `visualize`, `make_result`, `modify_before_fit`, `modify_after_fit` no-op methods so `af.AnalysisFactor` doesn't crash on pre-fit setup.

### v0.96 polish + Cannon cross-pollution root fix shipped earlier today

- Root docs version-bumped v0.92 → v0.96; Module 15 row in CLAUDE.md curriculum; Solutions/15_radial_arcs_SOLVED.ipynb stub
- 124 cross-example polluted dirs cleaned via diff-then-delete; 2 legitimate canonical fits committed (ggsa_direct, quad_direct_fit)
- `submit_cannon.slurm` SCOPED_OUTPUT_ROOT fix prevents recurrence
- `NEXT_STEPS.md` rewritten with v0.97 Tier-1 priorities

### Paper-reproduction program scoped + specced + planned (the big new thing)

User asked: "make a dedicated private directory to reproducing this paper... 2307.09271, 2602.20889, 2309.04535." Then added Nightingale+2023 (Abell 1201) as a future TODO and "use herculens directly and leverage cluster computing".

Brainstormed via `/brainstorming` skill. **6 design specs + 5 implementation plans** committed:

**Design specs** (`docs/superpowers/specs/`):
1. **Spec 00 — Shared Infrastructure**: J0946 data + Chen+2019 catalogue + autolens↔Herculens bridge + crossval framework + Cannon GPU runner + **AGEL Watson HST reduction pipeline** integration
2. **Spec 01 — P1 Li+2023 Population Cosmography** (laptop dual-sampler: autofit Nautilus + NumPyro NUTS)
3. **Spec 02 — P2 Ballard+2023 TSPL Jackpot Subhalo** (Cannon, both stacks, 6 fits)
4. **Spec 03 — P3 Li+2026 DSPL Jackpot IMF+NFW** (Cannon, both stacks, MGE + gNFW + free α + γ_inner)
5. **Spec 04 — Cross-Validation + Promotion** (4-way J0946 consistency, 2 new public Modules, 3 new Examples, HERCULENS_INTEGRATION.md, v0.98 tag)
6. **Spec 05 — Nightingale+2023 Abell 1201 UMBH** (FUTURE TODO, activation criteria documented)

**Implementation plans** (`docs/superpowers/plans/`, 5,034 lines total, 143 checkbox steps):
- Spec 00 plan: 2175 lines, 62 steps
- Spec 01 plan: 849 lines, 23 steps
- Spec 02 plan: 925 lines, 25 steps
- Spec 03 plan: 584 lines, 13 steps
- Spec 04 plan: 501 lines, 20 steps

Critical discoveries during spec-writing:
- The 161-lens sample for P1 is **Chen+2019** (arxiv 1809.09845, DOI 10.1093/mnras/stz1902), NOT my initial guess of SLACS/SL2S/BELLS union. Today's `private/2307_.../code/build_catalogue.py` (141 lenses via Vizier) was solving the wrong problem — refactored to `build_structural_enrichment.py` (auxiliary).
- **Herculens** (github.com/Herculens/herculens) is the JAX-native lens-modelling library used by P3; promotes to first-class second stack on Cannon GPU.
- **Watson AGEL HST reduction pipeline** (CWatson_AGEL_HST_reduction_scripts/, Harvard Dataverse DOI 10.7910/DVN/SPDDCE) is the AGEL group's canonical HST data prep with AGEL-tuned cosmic-ray rejection. Re-drizzle J0946 raw FLT files for consistent input across P2 + P3 reproductions.

---

## 2. Active state at handover

### Cannon

- **Job 13649652** `rarc_kin2`: RUNNING (radial_arc_smbh --part=with_kinematics)
- **Job 13649870** `qtd_h0_kin`: RUNNING (quad_time_delay --part=joint_fit_h0_kin)
- Both submitted with `--account=siag_lab --partition=siag --mem=192G`. Expected wall ~6-12h each.

### Working tree

- Clean modulo the two known Modules/04_Search_Chaining_SLaM/results/{chaining,slam}/ untracked dirs (these are sub-organizations of existing tracked SLaM results, not pollution)
- `private/` extensively populated:
  - `private/2307_09271_li2023_cosmography_population/` — STUB Chen+2019 catalog + working per-system likelihood + (now-flagged-as-wrong-direction) build_catalogue.py
  - `private/2309_04535_ballard2023_tspl_jackpot/` — PAPER_NOTES.md, download_hst.py, **9.8 GB of HST data pulled to `data/hst/`**
  - `private/2602_20889_li2026_dspl_imf_nfw/` — PAPER_NOTES.md, symlinks to P2 data
  - `private/STRATEGY.md` (overall program), `private/PROGRESS_2026_05_18.md` (session log)
  - `private/` is gitignored — only public-repo design specs + plans are committed

### Public commits since v0.96-alpha

```
e1cba99  specs: patch with paper code/data discovery audit
5e4e6d7  specs: AGEL Watson pipeline integration + Nightingale 2023 spec 05
40f3c99  plans: Spec 00 implementation plan (shared infrastructure)
e1cba99→latest  + 4 more plans + this handoff
```

All pushed to origin/main.

---

## 3. What's ready to execute

After compact, the next session can pick any of:

### Track A: audit + ship v0.97 Cannon results (if jobs land)

When 13649652 + 13649870 land:
1. `bash Modules/10_Cluster_Computing/scripts/pull_from_cannon.sh --go`
2. Audit each via `/autolens-fit-diagnostics`
3. If both strict-PASS:
   - Update `Examples/radial_arc_smbh/01_radial_arc_smbh.ipynb` §5 with the kinematic-break headline
   - Update `Examples/cosmography_joint_posterior/01_joint_posterior.ipynb` §4 with MSD-broken H0
   - Tag `v0.97-alpha`
4. If either FAIL: diagnose + document.

### Track B: execute Spec 00 plan (shared infra)

`docs/superpowers/plans/2026-05-18-paper-repro-00-shared-infrastructure.md` is the load-bearing plan. 62 checkbox steps across 8 phases:
1. scaffold + Herculens env
2. Chen+2019 catalog (STUB unblocks downstream; full table acquisition deferred)
3. J0946 data loader
4. Herculens bridge (5 profile-by-profile TDD cycles)
5. cross-validation framework
6. Cannon-side Herculens runner (siag_lab A100)
7. AGEL Watson HST pipeline integration
8. docs

Execute via `superpowers:subagent-driven-development` (one task per subagent) or `superpowers:executing-plans` (inline batched).

### Track C: Specs 01-03 execution (depend on Spec 00 substantially complete)

In order: 01 (laptop-only, cheapest), then 02 + 03 in parallel (both Cannon-heavy).

### Track D: Spec 04 promotion (depends on 01-03)

Final stretch — laptop only — that lands the public Module 16, 17, Example additions + v0.98 tag.

---

## 4. Open questions / blockers for next session

1. **Chen+2019 full 161-row Table 1 acquisition** — currently STUB at 10 rows in `build_chen2019_catalog.py`. Sources:
   - MNRAS supplementary materials (paywall — institutional access)
   - arxiv 1809.09845 e-print bundle (`tar -xf arxiv-e-print` may have machine-readable table)
   - Manual extraction from paper PDF Table 1
   - Email corresponding author (Yun Chen)

2. **MUSE cube for J0946+1006** — ESO archive manual download required. Programs to check via science portal: 0103.B-0743 (Smith/Collett TSPL follow-up), 094.B-0524 (Collett DSPL), 102.A-0950 (Sonnenfeld). Needed for P2 + P3 source-position likelihood.

3. **Watson pipeline raw FLT download** — Spec 00 §6.9 calls for running notebook 1-Downloader.ipynb on proposals 10886 + 11701 to get raw FLT files. We have HLA DRC products already (9.8 GB); raw FLT are smaller but separate download.

4. **Herculens install on Cannon** — first execution of Spec 00 Task 9 will exercise this. Validate GPU partition + A100 access via `salloc --account=siag_lab --partition=gpu --gres=gpu:a100:1`.

---

## 5. Repo state at handoff

- Branch: `main` at latest commit
- Tag: `v0.96-alpha` (2026-05-15); v0.97 + v0.98 pending
- Working tree: clean modulo expected untracked Modules/04 dirs
- Origin: `git@github.com:rodelcr/Learning_to_Autolens.git` (PUBLIC)
- Cannon repo: synced via push_to_cannon.sh after each commit
- `private/`: gitignored; ~10+ GB local-only; tracked in `private/PROGRESS_2026_05_18.md`

---

## 6. First actions next session

1. `git status` — should be clean
2. `ssh cannon "squeue -u rcordova"` — check 13649652 + 13649870 state (likely still running OR landed; if landed, see Track A above)
3. If Track A is ready: pull + audit + tag v0.97
4. Else: pick a plan track (B/C/D) per priority

The plans use checkbox syntax for tracking; the writing-plans skill recommends `superpowers:subagent-driven-development` for execution. For Spec 00 (62 steps), a subagent-per-phase pattern works well.

---

## 7. Key methodological discoveries this session

1. **Always check Data Availability sections + GitHub** before scaffolding a paper reproduction. Today's mistake: assumed P1's 161-lens sample was a SLACS/SL2S/BELLS union; the paper's Data Availability statement explicitly cites Chen+2019. Codified as `docs/superpowers/specs/2026-05-18-paper-repro-00-shared-infrastructure-design.md §6.8 Standing Protocol`.

2. **AGEL Watson HST pipeline** is the canonical reduction for cross-paper consistency. The AGEL group has tuned AstroDrizzle CR-rejection parameters specifically for AGEL imaging; HLA's generic defaults leave residual cosmic rays that bias MGE light decomposition + radial-arc detection. For J0946 reproductions specifically.

3. **Dual-stack methodology** is a publishable contribution in its own right. PyAutoLens (Nautilus) and Herculens (NUTS) cross-validation is the methodology contribution of the v0.98 program; documented in Spec 04 § "Tool Development Findings" + the public `HERCULENS_INTEGRATION.md` doc.

---

## 8. Files of note

```
docs/superpowers/specs/
  2026-05-18-paper-repro-00-shared-infrastructure-design.md
  2026-05-18-paper-repro-01-population-cosmography-design.md
  2026-05-18-paper-repro-02-tspl-jackpot-subhalo-design.md
  2026-05-18-paper-repro-03-dspl-jackpot-imf-nfw-design.md
  2026-05-18-paper-repro-04-crossval-promotion-design.md
  2026-05-18-paper-repro-05-nightingale2023-abell1201-design.md  (FUTURE)

docs/superpowers/plans/
  2026-05-18-paper-repro-00-shared-infrastructure.md      (2175 lines, 62 steps)
  2026-05-18-paper-repro-01-population-cosmography.md     ( 849 lines, 23 steps)
  2026-05-18-paper-repro-02-tspl-jackpot-subhalo.md       ( 925 lines, 25 steps)
  2026-05-18-paper-repro-03-dspl-jackpot-imf-nfw.md       ( 584 lines, 13 steps)
  2026-05-18-paper-repro-04-crossval-promotion.md         ( 501 lines, 20 steps)
                                                          ───────────────────────
                                                          5034 lines, 143 steps

private/ (gitignored, local-only)
  STRATEGY.md                                  (per-paper approach)
  PROGRESS_2026_05_18.md                       (session log)
  00_shared_infrastructure/                    (not yet scaffolded; Spec 00 plan creates it)
  2307_09271_li2023_cosmography_population/
  2309_04535_ballard2023_tspl_jackpot/         (9.8 GB HST data downloaded)
  2602_20889_li2026_dspl_imf_nfw/
```

**Compact-safe.** This handoff doc captures all state needed to resume.
